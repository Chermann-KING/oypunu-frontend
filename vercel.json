{
  "buildCommand": "npm run build",
  "installCommand": "npm install --legacy-peer-deps --verbose",
  "framework": "angular",
  "outputDirectory": "dist/oypunu-frontend",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
